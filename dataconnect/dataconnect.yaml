specVersion: "v1"
serviceId: "counter"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "counter-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
